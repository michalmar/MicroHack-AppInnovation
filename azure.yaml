# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: dotnet
metadata:
    template: azd-init@1.18.2
services:
    legocatalog-app:
        project: dotnet/src/LegoCatalog.App
        host: containerapp
        language: dotnet
resources:
    legocatalog-app:
        type: host.containerapp
        port: 8080
